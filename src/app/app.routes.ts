import { Routes } from '@angular/router';
import { ExpressionValidatorComponent } from './components/expression-validator/expression-validator.component';

export const routes: Routes = [
    {
        path: '',
        component:ExpressionValidatorComponent,
      },
];
